[Unit]
Description=Links mounted net fs to dir NET into home
ConditionPathIsDirectory=/mnt/nfs/%u

[Service]
ExecStart=/usr/bin/ln -s /mnt/nfs/%u %h/NET
Type=oneshot

[Install]
WantedBy=default.target
